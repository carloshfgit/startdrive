/**
 * Auth Feature Screens
 * 
 * Re-exporta as telas de autenticação do diretório legado.
 * Isso permite usar imports do novo local (features/auth/screens)
 * enquanto mantém compatibilidade com o código existente.
 */

// Re-export de screens existentes
export { LoginScreen } from '@/screens/auth/LoginScreen';
export { RegisterScreen } from '@/screens/auth/RegisterScreen';
